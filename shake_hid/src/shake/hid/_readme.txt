The hid module is responsible for Human Interface Devices,
which includes things like windows, keyboard, mouse and controller.
These things generally communicate through the OS using all kinds of events. 
To get access to these events in a cross platform way, we rely on GLFW.
