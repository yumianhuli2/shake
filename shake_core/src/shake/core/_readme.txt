The core module is responsible for the most basic functions, algorithms and datastructures.
It had no dependencies on other modules.
Instead, almost everything else is expected to rely on the core module.
